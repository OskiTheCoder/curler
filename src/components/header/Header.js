import React from 'react';
import '../../styles/header/styles.scss';

const Header = () => {
  return (
    <header>
      <h1>cURLer</h1>
      <p>
        Generate simple curl commands that are ready to be pasted into your
        terminal!
      </p>
    </header>
  );
};

export default Header;
